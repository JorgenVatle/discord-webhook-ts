on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout Git Repository'
        uses: actions/checkout@v2

      - name: 'Install modules'
        run: npm i

      - name: 'Run tests'
        run: npm t